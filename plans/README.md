# Implementation Plans

Generated by the `improve` skill (`deep`) on 2026-07-28, against commit
`c99aed7f` on branch `claude/client-multi-os-build-c1y599`.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Scope of the audit that produced these

- **Audited**: all hand-written Dart under `lib/` (332 files), `test/`,
  `Makefile`, `.github/workflows/**`, `analysis_options.yaml`, `pubspec.yaml` /
  `pubspec.lock`, and the Android/Windows/Linux/macOS platform config.
- **NOT audited**: the `hiddify-core/` Go submodule. It is **not checked out**
  on the machine where this audit ran (empty directory), so the entire Go core
  — the proxy engine, the gRPC service, `ray2sing`, and the vendored
  sing-box/xray-core forks — was out of reach. Anything below that touches the
  client↔core contract is therefore reasoned from the Dart side only.
- **Excluded by design**: generated code (`**/*.g.dart`, `**/*.freezed.dart`,
  `lib/gen/**`, `lib/hiddifycore/generated/**`, `test/drift/db/generated/**`).
- Every finding that became a plan was re-verified by reading the cited code
  directly, not taken from the audit agents' reports.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make lint, analysis and tests actually run in CI | P1 | M | — | DONE — analyzer backlog: 4689 findings (cascading from missing codegen; build_runner ^2.4.x incompatible with analyzer 7.6.x used by custom_lint ^0.7.x — test suite cannot run until resolved) |
| 002 | Stop writing proxy credentials into logs and crash telemetry | P1 | S | — | DONE — test file created; flutter test blocked by same codegen blocker as 001 |
| 003 | Fix the settings that never reach the core, and add a guard | P1 | M | 001 | DONE — 3 drifts fixed, drift guard test added (hardcoded key-shape mirror; flutter test blocked by codegen blocker) |
| 004 | Actually delete the profile temp files holding subscription credentials | P1 | M | 001 | DONE — upsertRemote + offlineUpdate fixed (task.run() inside try/finally); sidecar files deleted in parser. Manual step 5 not runnable in this env. |
| 005 | Stop re-sorting the proxy list / re-parsing the log buffer per tick | P2 | S | 001 | DONE — throttleTime(250ms, leading:true) added to proxy stream; deleted commented-out block; throttle added before log parse. Manual smoke check not runnable in this env. |
| 006 | Stop the proxy-list stream dying permanently when the core is slow to start | P1 | M | 001 | DONE — handleExceptions resumes with backoff; watchActiveGroups polls instead of completing empty. Step 4 (Go side) skipped per plan instructions. Manual test not runnable in this env. |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **001 first.** It declares `flutter_test` (currently commented out of
  `pubspec.yaml` and resolving only as a transitive of a *runtime* package), so
  until it lands the test suite compiles by accident. 003, 004 and 005 all rely
  on `flutter test` meaning something.
- **002 is independent** and can land in parallel with 001 — it is the highest
  user-impact item here and should not wait on tooling work.
- 003 and 004 are independent of each other.
- 005 is low risk and low urgency, not blocked by anything.
- **006 supersedes the `handleExceptions` entry** in "Findings recorded but NOT
  planned" below — that finding is now planned. 006 also touches
  `lib/hiddifycore/hiddify_core_service.dart`; if 005 and 006 are executed
  concurrently, note they touch different files and do not conflict.

## Second pass: the Go core (2026-07-29)

The `hiddify-core/` submodule was checked out after the first pass and audited
as a follow-up at submodule commit `8653861`. **That audit did not complete**:
all four category agents failed to infrastructure problems (two stalled, two
hit an account spend limit). What follows is only what was verified by hand
before stopping.

**Verified directly, became plan 006**: the proxy-list gRPC stream terminates
permanently when the core is slow to start — `MakeSureContextIsNew`
(`hiddify-core/v2/hcore/commands.go:84-99`) waits at most 10 × 500 ms = 5 s,
after which `AllProxiesInfoStream` (`v2/hcore/proxy_info.go:189-248`) skips its
whole body and returns `E.New("hiddify service not found")`; the Dart client
(`lib/hiddifycore/hiddify_core_service.dart:298-316`) rethrows into
`handleExceptions`, whose `onErrorReturnWith` completes the stream for good.

**Verified directly, NOT planned — monitoring startup amplification**:
`hiddify-sing-box/common/monitoring/outbound_monitoring.go` uses
`defaultWorkerCount = 10` (`:37`), `defaultURLTestTimeout = 5s` (`:39`), and
all non-group outbounds start `invalid: true`, so the first cycle tests every
one. `runCycle` (`:636-662`) loops over `m.urls` and, when a stage yields zero
successes, re-tests the **entire** tag list against the next fallback URL.
`ConnectionTestUrls` defaults to 4 URLs (`v2/config/builder.go:250`). For ~45
outbounds pointing at one unreachable server that is 45 × 4 = 180 tests at 10
concurrent × 5 s ≈ **90 seconds** of churn, all aimed at a single host. This
does **not** block the proxy list (the immediate `stream.Send` at
`proxy_info.go:196` proves it), so it is load and battery cost rather than the
slow-list cause. Worth its own plan; retry only the failed tags rather than the
whole list.

**NOT audited at all** (agents died before producing findings): the Go core's
security posture — in particular whether the Go side enforces the generated
secret on the loopback gRPC control channel, and the trust boundary of the two
elevated helper processes (`v2/hcore/tunnelservice/` on port 18020,
`v2/hcore/icmpservice/` on 18021). The Dart side connects with
`ChannelCredentials.insecure()` and never sends the secret it generates; whether
that is exploitable depends entirely on the Go side, which remains unread. Also
unaudited: `ray2sing`'s link parsers (including the Shadowsocks-2022/EIH path,
still the unexplained "SS2022 doesn't work in proxy mode" report), and general
lifecycle/state correctness in `v2/hcore`.

## Findings recorded but NOT planned

These were verified against the code but did not become plans in this pass —
either they need a maintainer decision first, or they lose to the five above on
leverage. Recorded so they are not re-audited from scratch.

### Security

- **Windows code-signing certificate is written to the workspace on PR-triggered
  runs.** `.github/workflows/build.yml:295-311` — the "Setup Windows Signing
  Properties" step is gated only on `startsWith(matrix.platform,'windows')`,
  **not** on `inputs.upload-artifact`, so a same-repo branch PR materializes the
  real PFX and its password into the workspace before PR-authored build scripts
  run (`:333-349`). Compounded by `permissions: write-all` (`ci.yml:30`,
  `build.yml:106`, `:461`, `:527`) and third-party actions pinned to mutable
  branches — `hiddify/signtool-code-sign-sha256@main` (`:353`) is handed the
  certificate. **Needs a maintainer decision** because it touches the release
  and signing pipeline, and because the correct response includes *rotating*
  the Windows signing certificate/password and the Android keystore
  credentials, not just re-gating the step.
- **Unquoted executable path in Windows URL-protocol registration.**
  `lib/core/router/deep_linking/url_protocol/windows_protocol.dart:18` —
  `'${executable ?? Platform.resolvedExecutable} ${args.join(' ')}'`. The
  arguments go through `_sanitize` (`:52-55`) which quotes them; the executable
  does not. With a default install path containing spaces this is the classic
  unquoted-service-path resolution order, under `HKCU`, rewritten on every
  launch. Fix is one call to the existing `_sanitize`. Small and safe — omitted
  only for lack of a Windows machine here to confirm a deep link still opens.
- **Android `allowBackup` is unset**, so the platform default (`true`) applies:
  `android/app/src/main/AndroidManifest.xml:27-34` has no `android:allowBackup`,
  `android:dataExtractionRules`, or `android:fullBackupContent`. Profiles
  (including subscription URLs, which embed per-user tokens) live in an
  unencrypted drift DB and plain files, so they are swept into Google account
  backup. One attribute; the trade-off (losing profile restore-on-reinstall) is
  a product call.
- **Third-party git dependency pinned to `HEAD`.** `pubspec.yaml:109-110`
  points `installed_apps` at `https://github.com/VB10/installed_apps` with no
  `ref:`; `pubspec.lock:925` confirms `ref: HEAD`. It drives the per-app-proxy
  feature (which apps get tunneled). The committed lockfile protects normal
  builds, but any `flutter pub upgrade` pulls that repo's default branch
  unreviewed. Fix: pin to the SHA already in the lockfile
  (`395d51876f9b5b4bc620d8549b05755c6f1966ba`), or fork it under `hiddify-com`
  as was already done for `circle_flags`.
- **Cleartext subscription fetches are silently allowed.**
  `lib/features/profile/data/profile_parser.dart:155-156` has a commented-out
  guard that used to reject `http://` subscription URLs. `dio_http_client.dart:129-152`
  lifts `uri.userInfo` into a `Basic` auth header, so over cleartext the
  subscription token is on the wire. The right fix is a warning-with-confirmation
  rather than a hard block (some providers legitimately serve cleartext) — which
  is a product decision.

### Correctness

- **`changeOptions` failure is discarded before the core starts.**
  `lib/features/connection/data/connection_repository.dart:133` —
  `await singbox.changeOptions(overridedOptions).run();` throws away the
  `Either`, so a core rejection of the settings is invisible and the app
  connects with the previous options while `_configOptionsSnapshot` records the
  ones that were never accepted. Fix is small; deferred because surfacing a
  previously-swallowed error will make some currently-"working" connects start
  failing loudly, which needs a decided UX.
- **Cancelled profile expansion writes the literal string `null` into the
  profile.** `lib/features/profile/data/profile_parser.dart:239-242` joins a
  `List<String?>` with `results.join("\n")`, and the guard
  `results.any((e) => e != null)` is inverted relative to intent (it should be
  "all non-null"). Closely related to plan 004 and in the same function; kept
  separate because it is a correctness bug, not a credential-lifetime one.
- **Nullable preferences can never be cleared.**
  `lib/core/utils/preferences_utils.dart:54-61` — the `switch (mapped)` has
  arms for `String`/`bool`/`int`/`double`/`List<String>` and a
  `_ => throw const FormatException("Invalid Type")` default, so `null` throws,
  is caught at `:62`, and `write` returns `false` leaving state unchanged.
  Affects `extraSecurityProfileId`, `unblockerProfileId`, `windowPosition`,
  `autoAppsSelectionLastUpdate`. Fix: handle `null` by calling
  `preferences.remove(key)`.
- **`handleExceptions` terminates long-lived streams.**
  `lib/core/utils/exception_handler.dart:22` uses rxdart's `onErrorReturnWith`,
  which emits the replacement item and then **completes** the stream. It is
  applied to every live watch in the app (logs, proxies, stats, active profile
  — 7 call sites). One transient gRPC hiccup during a core restart ends the
  stream for the process lifetime, which is a strong candidate for
  "UI stops updating until restart" reports. Deferred because the fix changes
  lifetime semantics at 7 sites and needs a backoff strategy, not a one-liner.

### Tests

- **Characterization tests for `ProfileParser`.**
  `lib/features/profile/data/profile_parser.dart` is the 3rd-highest-churn
  non-generated file, sits on two trust boundaries (untrusted subscription
  content, and the override blob handed to the core), and has five **pure
  static** functions with zero platform dependencies and zero coverage:
  `_parseHeadersFromContent` (`:271-287`), `profileOverride` (`:426-452`),
  `applyProfileOverride`/`_mergeJson` (`:454-477`), `protocol` (`:379-409`),
  `_parseSubscriptionInfo` (`:289-303`). Both existing tests pass `content: ''`
  (`profile_parser_test.dart:79`, `:134`), so the untrusted-content parser has
  literally never executed under test. A test file already exists to extend and
  no mocks are needed — this is the highest-leverage *test-writing* target in
  the repo and the natural next plan after 001.
- **`test/drift/db/migration_test.dart:49-72`** asserts `expect([], [])` — both
  data lists are empty with a generated `// TODO: Fill these lists`. Either fill
  or delete; it currently reads as coverage while verifying nothing.

### DX / docs

- **No `CLAUDE.md` / `AGENTS.md`.** Codegen must run (`make gen`) before
  anything compiles — 69 files declare a `.g.dart` part and 27 a
  `.freezed.dart` part, none committed, all gitignored. An agent that edits
  `lib/` and runs `flutter test` without codegen gets cascading
  "Target of URI doesn't exist" errors and may "fix" them by editing generated
  files. The team already writes agent-facing docs (`SLANG.md` is written as an
  agent command and auto-loaded via `.aider.conf.yml:388-389`) but only aider
  sees it.
- **`CONTRIBUTING.md` is wrong in four places.** It shows
  `Flutter 3.13.4 • Dart 3.1.2` as the version example (`:35-45`) while
  `pubspec.yaml:7-12` requires `flutter: ^3.38.5` — a contributor following the
  doc fails `flutter pub get` before writing a line. Also: a 404 core link
  (`:32`), `flutter_distributor` where the Makefile uses `fastforge` (`:96`),
  and a reference to `make prepare` which only `echo`s a list (`:64`).
  **No mention of `git submodule update --init --recursive` anywhere**, and
  `make android-prepare` is the first target that hard-fails without it.
- **`build.yml:107` sets `continue-on-error: true` on the `build` job**, and
  every platform except Windows is commented out (`:116-163`) — the Build gate
  currently reports green while proving very little.

### Performance

- **`changeProxy` and `_sortOutbounds` mutate state in place.**
  `lib/features/proxy/overview/proxies_overview_notifier.dart:212-217` and
  `:174-176` mutate the object already held by `state` and re-wrap the same
  instance, so previous and next state are identical objects and no consumer
  (Riverpod equality, `select`, any diff) can distinguish them. Related to plan
  005 but a state-identity change, so kept separate.
- **Widgets watch a whole provider to read one int.**
  `connection_button.dart:30-31`, `active_proxy_delay_indicator.dart:16`, and
  `connection_stats_card.dart:17` watch `activeProxyNotifierProvider` and use
  only `urlTestDelay` / `tagDisplay`. Because `OutboundInfo` carries byte
  counters that change every tick, equality never short-circuits and the home
  screen's animated connection button rebuilds at core tick rate permanently.
  Three one-line `.select(...)` changes. `active_proxy_card.dart:22` already
  does it correctly and is the in-repo exemplar.
- **System tray is re-registered on every core tick.**
  `lib/features/system_tray/notifier/system_tray_notifier.dart:36-42,52-54` —
  the tray notifier depends on a live per-tick stream and unconditionally
  issues `setIcon` + `setToolTip` + `setContextMenu` on every rebuild.
- **The per-app-proxy screen enumerates installed apps three times**, twice
  with icon bytes (`per_app_proxy_page.dart:59-60`, `:37-43`;
  `per_app_proxy_notifier.dart:34`).

### Migrations (verdicts, not plans)

- **freezed 2 → 3: worth doing.** 46 `@freezed` declarations across 27 files;
  31 need one keyword (`abstract` or `sealed`) added. Critically, there are
  **zero freezed-union `.when`/`.map` call sites** — all 9 `.when(` sites are
  `AsyncValue.when` from Riverpod and all `.map(` sites are `Iterable.map`. The
  team already migrated to `sealed class` + `switch`. Cheapest major-version
  debt in the repo.
- **Riverpod 2 → 3: do not do this now.** 155 of 332 files (47%), 933 `ref.*`
  call sites, 84 `StateNotifierProvider` instantiations, plus a forced
  `flutter_hooks` 0.20 → 0.21 bump (18 files, 68 hook sites). Riverpod 3 also
  enables automatic provider retry by default, which silently changes
  connection-failure UX in a VPN client. With ~3 tests as the regression net
  this is a gamble, and nothing forces it. **Cheap prep that is safe on v2
  today** (~1 hour): replace the 5 generated `*Ref` typedefs
  (`app_info_provider.dart:12`, `app_update_data_providers.dart:8`,
  `log_data_providers.dart:10,20`, `stats_data_providers.dart:8`) with plain
  `Ref`, and rewrite `lib/utils/riverpod_utils.dart:5` off `AutoDisposeRef`.
  Those are the two hardest breaks.
- **`cupertino_http` (`pubspec.yaml:80`) has zero Dart references** but is
  compiled into every iOS/macOS binary (`ios/Podfile.lock`,
  `Runner.xcodeproj/project.pbxproj:121`). Either finish wiring it as dio's
  Apple `HttpClientAdapter` or drop it.
- **`dart_mappable` is a third serialization stack for 3 files** — a whole
  extra `build_runner` pass. Convert to the freezed + `json_serializable`
  convention the other 27 files use.

## Findings considered and rejected

So nobody re-audits them:

- **Proxy list virtualization** — not a problem. `GridView.builder`,
  `SliverList.builder`, `ListView.builder` are used throughout; the only
  `ListView(children:)` sites are fixed-length settings pages.
- **N+1 database access** — none. `app_proxy_data_source.dart` uses
  `transaction()` / `db.batch()` and pushes filtering into SQL.
- **SQL injection** — drift's generated query builders throughout; the single
  raw statement (`lib/core/db/db.dart:73`) interpolates a compile-time table
  name, not user input.
- **Hardcoded credentials** — none found. The Sentry DSN is injected via
  `String.fromEnvironment` (`lib/core/model/environment.dart:7`); Android
  signing values come from a gitignored `key.properties`. The
  `"password": "admin"` at `json_editor.dart:219` is a UI schema example.
- **TLS bypass** — none. No `badCertificateCallback` anywhere; Android
  cleartext is correctly scoped to `127.0.0.1`
  (`android/app/src/main/res/xml/network_security_config.xml:3-5`); no iOS ATS
  exceptions.
- **README translation drift** — checked and not stale; all six were updated
  together on 2026-02-27 and carry matching heading counts.
- **`vclibs` looks unused but is load-bearing** — zero Dart references, but it
  is a Windows native plugin registered via
  `windows/flutter/generated_plugin_registrant.cc:17`. **Do not remove it**; a
  dependency-pruning pass would break Windows VC++ runtime provisioning
  silently.
- **26 direct `Platform.isX` sites bypass the `PlatformUtils` wrapper** — the
  wrapper only adds `!kIsWeb` guards and this app ships no web target.
  Cosmetic; not worth 26 edits.
- **`QUERY_ALL_PACKAGES` permission** (`AndroidManifest.xml:23-25`) — required
  by per-app proxy; conventional for this app class.
- **Route-rule Android app picker being fully commented out** — commit
  `7b6b203a` shows this was a deliberate deprecation in favour of
  `PerAppProxyPage`. Settled decision, not a finding (the dead code is worth
  deleting, which is a different category).
- **`protocol_handler` commented out of `pubspec.yaml`** — commit `b006ad92`
  shows a deliberate migration to `app_links`. Working as intended.
- **`lib/features/profile/details/json_editor.dart` (1699 lines)** — a
  deliberate vendored fork of `json_editor_flutter` 1.4.2 with protocol-specific
  additions, not accidental dead code. Worth a provenance comment, not removal.

## Direction findings

Options for the maintainer, not defects. Effort here is a design/spike bracket,
deliberately coarser than the bug estimates above. Presented in the session
output rather than duplicated here; the four best-grounded were:

1. **Profiles are the one entity with import but no export** — config options,
   route rules, and per-app-proxy all ship symmetric clipboard+file
   import/export; profiles ship neither. Strong evidence, but any profile
   backup format holds subscription tokens, so passphrase encryption is a
   precondition, not a nice-to-have.
2. **An extension host already exists in the core with no client**
   (`android/app/src/main/protos/extension/extension_service.proto:11-18`,
   Dart stubs generated, zero consumers) — would let circumvention tactics ship
   without a store release. High risk: it is a server-driven-UI channel.
3. **Desktop `--start` / `--stop` flags are advertised and plumbed but dropped**
   — `linux/packaging/appimage/make_config.yaml:16-25` declares the actions,
   both native runners forward argv, and `lib/main.dart:7` declares
   `main()` with no `List<String> args`. Gated on fixing the desktop gRPC
   channel first (see below).
4. **The first-run free-config fetch is a single hardcoded GitHub URL with no
   fallback** (`lib/features/profile/notifier/profile_notifier.dart:191-199`),
   and `test.configs/README.md` documents the workaround in the maintainers' own
   words: you need another VPN for first-time use. Real friction at the funnel's
   front door — but every fallback added is a new fingerprintable callout from
   an unprotected device, so "better failure messaging + offline QR/file import"
   may beat "more network attempts".

Also surfaced and worth a maintainer decision on its own merits: **the desktop
gRPC control channel to the core is unauthenticated** —
`lib/hiddifycore/core_interface/core_interface_desktop.dart:74`, `:112` use
`ChannelCredentials.insecure()` and `SetupMode.GRPC_NORMAL_INSECURE`, a 100-char
`secret` is generated at `:66` and never sent, and the `MTLSChannelCredentials`
block is commented out at `:113-116`. Mobile *does* use mTLS
(`core_interface_mobile.dart:40-42`), so desktop is the outlier. Confirming
whether the Go core independently enforces the secret requires the submodule,
which was not available for this audit.
